'use strict'; // Permet d'activer le mode strict pour éviter les erreurs de portée des variables (Slide 44).

// f1: Affiche le nom (variable de type string). 
// Concept : Déclaration de variable et type string (Slide 36).
function f1() {
  let nom = "John";
  console.log(nom); // Affichage console (Slide 33).
}

// f2: Affiche le prénom (variable de type string).
// Concept : Déclaration de variable et type string (Slide 36).
function f2() {
  let prenom = "Doe";
  console.log(prenom); // Affichage console (Slide 33).
}

// f3: Affiche le nom et le prénom concaténés (string).
// Concepts : Déclaration de variable, concaténation string (Slide 36).
function f3() {
  let fullName = "John-Doe";
  console.log(fullName); // Affichage console (Slide 33).
}

// f4: Affiche l'âge (variable de type number).
// Concepts : Déclaration de variable et type number (Slide 36).
function f4() {
  let age = 30;
  console.log(age); // Affichage console (Slide 33).
}

// f5: Affiche une note numérique.
// Concepts : Déclaration de variable et type number (Slide 36).
function f5() {
  let note = 15;
  console.log(note); // Affichage console (Slide 33).
}

// f6: Affiche la valeur de Pi (constante numérique).
// Concepts : Déclaration de variable, constante mathématique (Slide 36).
function f6() {
  let pi = 3.14;
  console.log(pi); // Affichage console (Slide 33).
}
